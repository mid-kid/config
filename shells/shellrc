# RC file for all the shells
export EDITOR='vim'
export VISUAL=$EDITOR
export PAGER='less'
alias vi='vim'
alias ls='ls --color=auto'
alias httrack='httrack -%v'
alias du='du -h'
alias df='df -h'
alias free='free -h'

# Load custom shell functions
for s in ~/.shellfuncs/*.sh; do
    source $s
done
