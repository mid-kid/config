#!/bin/sh

case "$1" in
    enable)
        ln -s "/etc/sv/$2" /var/service
        ;;
    disable)
        /usr/bin/sv stop "$2"
        rm "/var/service/$2"
        ;;
    *)
        /usr/bin/sv $@
        ;;
esac
